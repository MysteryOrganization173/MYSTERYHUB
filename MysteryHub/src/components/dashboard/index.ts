/**
 * Dashboard components — imports go here when building the dashboard.
 *
 * Planned components:
 *   - DashboardShell      — page wrapper with sidebar/header
 *   - DashboardHeader     — page title + breadcrumb
 *   - DashboardSidebar    — left nav
 *   - StatsCard           — metric cards (orders, referrals, spend)
 *   - OrdersTable         — paginated orders list
 *   - ReferralsPanel      — referral code + earnings breakdown
 */
export {};
