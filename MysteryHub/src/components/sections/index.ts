/**
 * Page section components — full-width composable page sections.
 *
 * Planned components:
 *   - HeroSection         — homepage hero with headline + CTA
 *   - FeaturedBundles     — horizontally scrollable featured items
 *   - HowItWorksSection   — 3-step explainer
 *   - CategoriesSection   — browse by category grid
 *   - TestimonialsSection — social proof carousel
 *   - StatsSection        — trust metrics (bundles sold, customers, etc.)
 *   - NewsletterSection   — email capture banner
 *   - CTASection          — bottom of page call-to-action
 */
export {};
