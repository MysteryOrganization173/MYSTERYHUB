/**
 * Card components — specialized cards for bundles, reviews, stats.
 *
 * Planned components:
 *   - BundleCard          — product tile (image, tier badge, price, CTA)
 *   - BundleCardSkeleton  — loading placeholder
 *   - ReviewCard          — star rating + user avatar + review text
 *   - StatCard            — key metric display (used in dashboard/marketing)
 *   - TestimonialCard     — social proof card
 *   - CategoryCard        — category browse tile
 *   - TierCard            — bundle tier selector
 */
export {};
